/* jshint -W097 */ // Disable warning 'use function form of use strict'
/* globals  console:false */ // defines as a read only global for jsHint
/* globals require:false */
/* globals module */
"use strict";

/**
This module defines the structure tghat contains the website configuration
**/

var config = {
  lastUpdated : "30/4/2016",
  pages: [
    "welcome","profile","etc ..."
  ]
};

module.exports = config;
